---
phase: 01-ad-launch-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - src/app/layout.tsx
  - src/app/(main)/layout.tsx
  - src/app/(landing)/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - public/sitemap.xml
  - public/robots.txt
  - .env.local
  - .env.example
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05]

must_haves:
  truths:
    - "next.config.ts has no static export, no basePath, no GITHUB_PAGES conditional"
    - "Existing pages (/, /about, /consultation, etc.) still render with Navbar and Footer"
    - "sitemap.xml returns URLs with squaremind.in domain"
    - "robots.txt references squaremind.in/sitemap.xml"
    - "API routes are enabled (no static export blocking them)"
    - "(landing) route group exists for distraction-free pages"
  artifacts:
    - path: "next.config.ts"
      provides: "Clean Vercel config without GitHub Pages hacks"
      contains: "nextConfig"
    - path: "src/app/(main)/layout.tsx"
      provides: "Layout with Navbar, Footer, WhatsAppFloat, StickyMobileCTA"
      exports: ["default"]
    - path: "src/app/(landing)/layout.tsx"
      provides: "Minimal landing page layout without nav/footer"
      exports: ["default"]
    - path: "src/app/layout.tsx"
      provides: "Root layout with fonts, global CSS, JSON-LD only"
      exports: ["default", "metadata"]
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap with squaremind.in URLs"
      exports: ["default"]
    - path: "src/app/robots.ts"
      provides: "Dynamic robots.txt with squaremind.in"
      exports: ["default"]
  key_links:
    - from: "src/app/(main)/layout.tsx"
      to: "src/components/Navbar"
      via: "import and render"
      pattern: "import Navbar"
    - from: "src/app/layout.tsx"
      to: "src/app/(main)/layout.tsx"
      via: "Next.js route group nesting"
      pattern: "children"
---

<objective>
Remove GitHub Pages static export configuration, restructure the app into route groups for layout isolation, and replace static sitemap/robots files with dynamic Next.js metadata API equivalents.

Purpose: Unlocks API routes on Vercel (blocked by static export) and creates the (landing) route group needed for the distraction-free Tri-City landing page. This is the foundation everything else depends on.
Output: Clean next.config.ts, route group structure with (main) and (landing), dynamic sitemap.ts and robots.ts, environment variable template.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ad-launch-pipeline/01-RESEARCH.md
@.planning/phases/01-ad-launch-pipeline/01-CONTEXT.md

<interfaces>
<!-- Current root layout that must be refactored -->
From src/app/layout.tsx:
```typescript
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import WhatsAppFloat from "@/components/WhatsAppFloat";
import StickyMobileCTA from "@/components/StickyMobileCTA";

// Currently renders Navbar, Footer, WhatsAppFloat, StickyMobileCTA in root layout
// These must move to (main)/layout.tsx
```

From next.config.ts:
```typescript
const isGitHubPages = process.env.GITHUB_PAGES === "true";
const nextConfig: NextConfig = {
  ...(isGitHubPages && {
    output: "export",
    basePath: "/SquareMind-Test",
    images: { unoptimized: true },
  }),
};
// This entire conditional must be removed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean next.config.ts and create environment variable template</name>
  <files>next.config.ts, .env.example, .env.local</files>
  <action>
1. Replace next.config.ts with a clean config: remove the `isGitHubPages` conditional, `output: "export"`, `basePath: "/SquareMind-Test"`, and `images: { unoptimized: true }`. The config should be a simple empty NextConfig object.

2. Create `.env.example` with all Phase 1 environment variables (comments explaining where to get each value):
   - NEXT_PUBLIC_SUPABASE_URL (existing)
   - NEXT_PUBLIC_SUPABASE_ANON_KEY (existing)
   - SUPABASE_SERVICE_ROLE_KEY (from Supabase Dashboard > Settings > API — NOT prefixed with NEXT_PUBLIC_)
   - NEXT_PUBLIC_META_PIXEL_ID (from Meta Events Manager — leave blank for now)
   - NEXT_PUBLIC_GA4_ID (from GA4 Admin — leave blank for now)
   - NEXT_PUBLIC_CALENDLY_URL (e.g. https://calendly.com/squaremind/strategy-call — leave blank for now)
   - NEXT_PUBLIC_WHATSAPP_NUMBER (WhatsApp Business number — leave blank for now)
   - NEXT_PUBLIC_SITE_URL=https://squaremind.in

3. If `.env.local` exists, add the new env vars (with empty placeholder values) to it without overwriting existing values. If it does not exist, copy from .env.example.
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && grep -c "GITHUB_PAGES\|output.*export\|basePath" next.config.ts | grep "^0$" && echo "CLEAN" || echo "STILL_DIRTY"</automated>
  </verify>
  <done>next.config.ts has zero references to GITHUB_PAGES, static export, or basePath. .env.example lists all Phase 1 env vars with documentation comments.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure app into (main) and (landing) route groups</name>
  <files>src/app/layout.tsx, src/app/(main)/layout.tsx, src/app/(landing)/layout.tsx</files>
  <action>
1. Create `src/app/(main)/layout.tsx`:
   - Import and render Navbar, Footer, WhatsAppFloat, StickyMobileCTA (exactly what the current root layout does for the body content)
   - Wrap children in `<main className="pt-20">`
   - This is a simple layout wrapper, NOT a root layout (no html/body tags)

2. Create `src/app/(landing)/layout.tsx`:
   - Minimal layout: just renders `<main>{children}</main>` with no Navbar, Footer, or other site chrome
   - This provides the distraction-free environment for landing pages

3. Modify `src/app/layout.tsx` (root layout):
   - KEEP: html tag, head tag, body tag, font setup, metadata export, JSON-LD script, globals.css import
   - REMOVE: Navbar, Footer, WhatsAppFloat, StickyMobileCTA imports and rendering
   - The body should only contain `{children}` (no wrapping main tag, no nav components)

4. Move ALL existing page directories into `(main)/`:
   - Move: about, case-studies, consultation, faq, frameworks, insights, privacy, properties, research, terms, tools
   - Move: page.tsx (homepage) into (main)/page.tsx
   - Do NOT move: layout.tsx (root), globals.css, favicon.ico, sitemap.ts, robots.ts
   - URLs do NOT change because route group names in parentheses are excluded from the URL path

5. Verify no relative import paths broke by checking for `../` imports in moved files. All imports should use `@/` alias which resolves to `src/` and remains valid regardless of directory depth.
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && test -f src/app/\(main\)/layout.tsx && test -f src/app/\(landing\)/layout.tsx && test -f src/app/\(main\)/page.tsx && ! grep -q "Navbar" src/app/layout.tsx && echo "ROUTE_GROUPS_OK" || echo "ROUTE_GROUPS_FAIL"</automated>
  </verify>
  <done>Root layout contains only html/body/fonts/CSS. (main) layout wraps children with Navbar+Footer+WhatsAppFloat+StickyMobileCTA. (landing) layout is minimal. All existing pages moved to (main)/ with unchanged URLs. No broken imports.</done>
</task>

<task type="auto">
  <name>Task 3: Replace static sitemap.xml and robots.txt with dynamic metadata API</name>
  <files>src/app/sitemap.ts, src/app/robots.ts, public/sitemap.xml, public/robots.txt</files>
  <action>
1. DELETE `public/sitemap.xml` and `public/robots.txt` — these static files take precedence over the dynamic metadata API and currently reference the wrong domain.

2. Create `src/app/sitemap.ts`:
   - Export a default function returning `MetadataRoute.Sitemap`
   - Base URL: `https://squaremind.in`
   - Include all existing pages: /, /about, /consultation, /research, /insights, /tools, /frameworks, /properties, /case-studies, /faq, /privacy, /terms
   - Include the new landing page: /invest/tri-city
   - Set priority: 1.0 for homepage, 0.9 for consultation and landing page, 0.8 for content pages, 0.3 for legal pages
   - Set lastModified to `new Date()` for all entries

3. Create `src/app/robots.ts`:
   - Export a default function returning `MetadataRoute.Robots`
   - Rules: allow all for all user agents
   - Sitemap URL: `https://squaremind.in/sitemap.xml`

4. Verify the build still works: run `npm run build` (or `npx next build`) to confirm no errors from the route group migration or config changes.
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && test -f src/app/sitemap.ts && test -f src/app/robots.ts && ! test -f public/sitemap.xml && ! test -f public/robots.txt && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Static sitemap.xml and robots.txt deleted from /public. Dynamic sitemap.ts and robots.ts created with squaremind.in domain. `next build` completes without errors.</done>
</task>

</tasks>

<verification>
1. `next build` succeeds with no errors
2. `grep -r "GITHUB_PAGES\|basePath\|output.*export" next.config.ts` returns nothing
3. `src/app/(main)/layout.tsx` contains Navbar and Footer imports
4. `src/app/(landing)/layout.tsx` does NOT contain Navbar or Footer
5. `src/app/layout.tsx` does NOT contain Navbar or Footer
6. All existing page directories exist under `src/app/(main)/`
7. `public/sitemap.xml` and `public/robots.txt` do NOT exist
8. `src/app/sitemap.ts` and `src/app/robots.ts` DO exist
</verification>

<success_criteria>
- The app builds successfully on Vercel with API routes enabled (no static export)
- Existing pages render with Navbar and Footer via the (main) route group
- A future page in (landing)/ would render without Navbar/Footer
- sitemap.xml serves squaremind.in URLs
- robots.txt references squaremind.in/sitemap.xml
</success_criteria>

<output>
After completion, create `.planning/phases/01-ad-launch-pipeline/01-01-SUMMARY.md`
</output>
