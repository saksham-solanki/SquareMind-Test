---
phase: 01-ad-launch-pipeline
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/utm.ts
  - src/lib/supabase-server.ts
  - src/lib/meta-pixel.ts
  - src/app/api/leads/route.ts
  - src/components/TriCityForm.tsx
  - src/components/UTMCapture.tsx
autonomous: true
requirements: [LEAD-05, LEAD-06, LEAD-08, LAND-03]

must_haves:
  truths:
    - "UTM params from URL are captured into sessionStorage on page load"
    - "POSTing valid lead data to /api/leads returns 200 and inserts into Supabase leads table"
    - "POSTing invalid data (missing name/email/phone) to /api/leads returns 400"
    - "Form submission includes UTM params and source page URL in the payload"
    - "TriCityForm collects name, email, phone, location dropdown, budget dropdown, and hidden UTM fields"
  artifacts:
    - path: "src/lib/utm.ts"
      provides: "UTM capture and retrieval from sessionStorage"
      exports: ["captureUTMParams", "getUTMParams"]
    - path: "src/lib/supabase-server.ts"
      provides: "Server-side Supabase client with service role key"
      exports: ["supabaseAdmin"]
    - path: "src/lib/meta-pixel.ts"
      provides: "Meta Pixel event tracking helper"
      exports: ["trackEvent"]
    - path: "src/app/api/leads/route.ts"
      provides: "POST endpoint for lead form submissions"
      exports: ["POST"]
    - path: "src/components/TriCityForm.tsx"
      provides: "Landing page lead capture form with all fields"
      exports: ["default"]
    - path: "src/components/UTMCapture.tsx"
      provides: "Client component that captures UTMs on mount"
      exports: ["default"]
  key_links:
    - from: "src/components/TriCityForm.tsx"
      to: "/api/leads"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/leads"
    - from: "src/components/TriCityForm.tsx"
      to: "src/lib/utm.ts"
      via: "getUTMParams() call on submit"
      pattern: "getUTMParams"
    - from: "src/app/api/leads/route.ts"
      to: "src/lib/supabase-server.ts"
      via: "import supabaseAdmin"
      pattern: "supabaseAdmin.*from.*leads.*insert"
---

<objective>
Build the lead capture pipeline: UTM tracking utilities, server-side Supabase client, API route for form submissions, and the landing page form component.

Purpose: This is the revenue-critical data pipeline. Every Meta ad click must result in captured lead data (name, email, phone, location, budget, UTMs) stored in Supabase. Without this, ad spend has zero measurable ROI.
Output: Working /api/leads endpoint, TriCityForm component ready to embed in landing page, UTM capture utilities.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ad-launch-pipeline/01-RESEARCH.md
@.planning/phases/01-ad-launch-pipeline/01-CONTEXT.md
@.planning/phases/01-ad-launch-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- Existing Supabase client pattern to follow -->
From src/lib/supabase.ts:
```typescript
import { createClient } from "@supabase/supabase-js";
// Uses NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY
```

<!-- Existing form pattern to adapt -->
From src/components/HeroForm.tsx:
```typescript
"use client";
// Uses useState for loading/submitted states
// Shows success message after submit
// Tailwind utility classes with custom theme colors
```

<!-- Supabase leads table schema (must be created in Supabase dashboard) -->
SQL schema from research:
```sql
CREATE TABLE public.leads (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  name text,
  email text NOT NULL,
  phone text,
  budget_range text,
  location text,
  source_page text,
  form_type text NOT NULL DEFAULT 'landing_tri_city',
  utm_source text, utm_medium text, utm_campaign text, utm_content text, utm_term text,
  created_at timestamptz DEFAULT now()
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create utility libraries and API route for lead capture</name>
  <files>src/lib/utm.ts, src/lib/supabase-server.ts, src/lib/meta-pixel.ts, src/app/api/leads/route.ts</files>
  <action>
1. Create `src/lib/utm.ts`:
   - Export `captureUTMParams()`: reads utm_source, utm_medium, utm_campaign, utm_content, utm_term from `window.location.search` (URLSearchParams) and stores each in sessionStorage (only if the value exists in the URL, do not overwrite with null)
   - Export `getUTMParams()`: returns an object with all 5 UTM keys read from sessionStorage (values may be null)
   - Guard both functions with `typeof window === "undefined"` checks for SSR safety
   - Define `UTM_KEYS` as a const array: `["utm_source", "utm_medium", "utm_campaign", "utm_content", "utm_term"]`

2. Create `src/lib/supabase-server.ts`:
   - Create and export a Supabase client using `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` (NO NEXT_PUBLIC_ prefix on service role key)
   - Export as `supabaseAdmin`
   - This file should ONLY be imported in server-side code (API routes, server components)

3. Create `src/lib/meta-pixel.ts`:
   - Export `trackEvent(eventName: string, params?: Record<string, unknown>)`: calls `window.fbq("track", eventName, params)` if fbq exists
   - Declare `window.fbq` type globally (extend Window interface)
   - Guard with `typeof window !== "undefined"` check

4. Create `src/app/api/leads/route.ts`:
   - Export async `POST(request: Request)` handler
   - Parse JSON body
   - Validate required fields: name, email, phone — return 400 with `{ error: "Missing required fields: name, email, phone" }` if any are missing
   - Validate email format with a basic regex — return 400 if invalid
   - Insert into Supabase `leads` table using `supabaseAdmin` with fields: name, email, phone, budget_range, location, source_page, form_type, utm_source, utm_medium, utm_campaign, utm_content, utm_term
   - On Supabase error: log error server-side, return 500 with `{ error: "Failed to save lead" }`
   - On success: return 200 with `{ success: true }`
   - Set form_type default to "landing_tri_city" if not provided
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && test -f src/lib/utm.ts && test -f src/lib/supabase-server.ts && test -f src/lib/meta-pixel.ts && test -f src/app/api/leads/route.ts && grep -q "supabaseAdmin" src/app/api/leads/route.ts && grep -q "SUPABASE_SERVICE_ROLE_KEY" src/lib/supabase-server.ts && ! grep -q "NEXT_PUBLIC.*SERVICE_ROLE" src/lib/supabase-server.ts && echo "PIPELINE_OK" || echo "PIPELINE_FAIL"</automated>
  </verify>
  <done>UTM utility captures and retrieves params from sessionStorage. supabase-server.ts uses service role key (NOT prefixed with NEXT_PUBLIC_). meta-pixel.ts exposes trackEvent helper. API route validates input, inserts to Supabase, returns proper status codes.</done>
</task>

<task type="auto">
  <name>Task 2: Build TriCityForm and UTMCapture components</name>
  <files>src/components/TriCityForm.tsx, src/components/UTMCapture.tsx</files>
  <action>
1. Create `src/components/UTMCapture.tsx`:
   - "use client" component
   - On mount (useEffect), call `captureUTMParams()` from `@/lib/utm`
   - Renders nothing (returns null) — purely a side-effect component
   - This component will be placed in the landing page to capture UTMs on arrival

2. Create `src/components/TriCityForm.tsx`:
   - "use client" component
   - Form fields per user decision:
     a. Name — text input, required
     b. Email — email input, required
     c. Phone/WhatsApp — tel input, required
     d. Location — dropdown (select) with placeholder "Where do you currently live?" and options: Mumbai, Delhi NCR, Bangalore, Hyderabad, Pune, Chennai, Kolkata, Chandigarh, Mohali, Panchkula, Gurgaon, Noida, Jaipur, Ahmedabad, Lucknow, Dubai (UAE), USA, UK, Canada, Singapore, Australia, Other
     e. Investment Budget — dropdown: "₹50L – ₹1Cr", "₹1Cr – ₹3Cr", "₹3Cr – ₹5Cr", "₹5Cr – ₹10Cr", "₹10Cr+"
   - State management: useState for each field, loading state, submitted state, error state
   - On submit:
     a. Set loading true
     b. Call `getUTMParams()` from `@/lib/utm`
     c. POST to `/api/leads` with all form fields + UTM params + `source_page: window.location.href` + `form_type: "landing_tri_city"`
     d. On success: set submitted true, call `trackEvent("Lead")` from `@/lib/meta-pixel`
     e. On error: set error message, set loading false
   - Success state: show a thank-you message ("Thank you! We'll reach out within 24 hours." or similar)
   - Styling: use existing Tailwind design system (bg-cream, text-ink, border-sage, etc.), mobile-first responsive, match SquareMind's editorial voice
   - Inputs should have proper labels, placeholders, and focus states
   - Submit button text: "Get Your Free Investment Guide" or similar (change to spinner/loading state while submitting)
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && test -f src/components/TriCityForm.tsx && test -f src/components/UTMCapture.tsx && grep -q "api/leads" src/components/TriCityForm.tsx && grep -q "trackEvent" src/components/TriCityForm.tsx && grep -q "getUTMParams" src/components/TriCityForm.tsx && grep -q "captureUTMParams" src/components/UTMCapture.tsx && echo "FORM_OK" || echo "FORM_FAIL"</automated>
  </verify>
  <done>TriCityForm renders all required fields (name, email, phone, location dropdown, budget dropdown), submits to /api/leads with UTM params and source page, fires Meta Pixel Lead event on success, shows loading and success states. UTMCapture captures UTM params on mount.</done>
</task>

</tasks>

<verification>
1. `src/lib/utm.ts` exports captureUTMParams and getUTMParams
2. `src/lib/supabase-server.ts` uses SUPABASE_SERVICE_ROLE_KEY (no NEXT_PUBLIC_ prefix)
3. `src/app/api/leads/route.ts` exports POST, validates required fields, inserts to Supabase
4. `src/components/TriCityForm.tsx` has all 5 form fields, calls /api/leads, fires trackEvent("Lead")
5. `src/components/UTMCapture.tsx` calls captureUTMParams on mount
6. `npx next build` succeeds
</verification>

<success_criteria>
- API route at /api/leads accepts POST with lead data and inserts into Supabase
- Invalid requests (missing name/email/phone) return 400
- TriCityForm component is ready to embed in the landing page
- UTM params are captured from URL into sessionStorage on page load
- Meta Pixel Lead event fires on successful form submission
</success_criteria>

<output>
After completion, create `.planning/phases/01-ad-launch-pipeline/01-02-SUMMARY.md`
</output>
