---
phase: 01-ad-launch-pipeline
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/app/(landing)/invest/tri-city/page.tsx
  - src/app/layout.tsx
  - src/components/MetaPixel.tsx
  - src/components/CalendlyButton.tsx
autonomous: false
requirements: [LAND-01, LAND-02, LAND-04, LAND-05, BOOK-03, ANAL-02, ANAL-03]
user_setup:
  - service: supabase
    why: "Lead data storage"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard > Settings > API > service_role key (secret)"
    dashboard_config:
      - task: "Run the leads table SQL from 01-RESEARCH.md to create the leads table and RLS policies"
        location: "Supabase Dashboard > SQL Editor"
  - service: meta-pixel
    why: "Ad conversion tracking"
    env_vars:
      - name: NEXT_PUBLIC_META_PIXEL_ID
        source: "Meta Events Manager > Data Sources > Create Pixel"
  - service: ga4
    why: "Site analytics"
    env_vars:
      - name: NEXT_PUBLIC_GA4_ID
        source: "GA4 Admin > Data Streams > Measurement ID (G-XXXXXXXXXX)"
  - service: calendly
    why: "Booking integration"
    env_vars:
      - name: NEXT_PUBLIC_CALENDLY_URL
        source: "Calendly > Event Types > copy event link (e.g. https://calendly.com/squaremind/strategy-call)"
  - service: whatsapp
    why: "Business chat"
    env_vars:
      - name: NEXT_PUBLIC_WHATSAPP_NUMBER
        source: "WhatsApp Business phone number"

must_haves:
  truths:
    - "Visiting /invest/tri-city shows a mobile-optimized landing page with NO Navbar or Footer"
    - "Landing page has educational content about Tri-City 2026 investment opportunity"
    - "Landing page form submits lead data to Supabase via /api/leads"
    - "Meta Pixel fires ViewContent on landing page load"
    - "Meta Pixel fires Lead event on form submission"
    - "Calendly popup opens when 'Book Your Free Strategy Call' button is clicked"
    - "Meta Pixel fires Schedule event when Calendly booking completes"
    - "GA4 tracks pageviews site-wide"
  artifacts:
    - path: "src/app/(landing)/invest/tri-city/page.tsx"
      provides: "Tri-City investment guide landing page"
      min_lines: 100
      exports: ["default", "metadata"]
    - path: "src/components/MetaPixel.tsx"
      provides: "Meta Pixel script loader and initialization"
      exports: ["default"]
    - path: "src/components/CalendlyButton.tsx"
      provides: "Calendly popup trigger with Schedule event tracking"
      exports: ["default"]
  key_links:
    - from: "src/app/(landing)/invest/tri-city/page.tsx"
      to: "src/components/TriCityForm"
      via: "import and render in page"
      pattern: "TriCityForm"
    - from: "src/app/(landing)/invest/tri-city/page.tsx"
      to: "src/components/CalendlyButton"
      via: "import and render as CTA"
      pattern: "CalendlyButton"
    - from: "src/app/layout.tsx"
      to: "src/components/MetaPixel"
      via: "render in root layout body"
      pattern: "MetaPixel"
    - from: "src/components/CalendlyButton.tsx"
      to: "src/lib/meta-pixel"
      via: "trackEvent('Schedule') on booking"
      pattern: "trackEvent.*Schedule"
---

<objective>
Build the Tri-City investment guide landing page, integrate Meta Pixel site-wide, add GA4 tracking, and wire up Calendly popup booking -- completing the full ad-to-lead pipeline.

Purpose: This is the endpoint of the Meta ad funnel. A visitor clicks an ad, lands on this page, reads about Tri-City investment opportunities, submits the form or books a call, and every action is tracked by Meta Pixel for ad optimization. Without this page, there is nothing to run ads to.
Output: Complete landing page at /invest/tri-city, Meta Pixel firing ViewContent/Lead/Schedule events, GA4 tracking, Calendly booking integration.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ad-launch-pipeline/01-RESEARCH.md
@.planning/phases/01-ad-launch-pipeline/01-CONTEXT.md
@.planning/phases/01-ad-launch-pipeline/01-01-SUMMARY.md
@.planning/phases/01-ad-launch-pipeline/01-02-SUMMARY.md

<interfaces>
<!-- Components available from Plan 02 -->
From src/components/TriCityForm.tsx:
```typescript
"use client";
export default function TriCityForm() { ... }
// Renders complete lead capture form, submits to /api/leads
// Fires trackEvent("Lead") on success
```

From src/components/UTMCapture.tsx:
```typescript
"use client";
export default function UTMCapture() { ... }
// Captures UTM params from URL into sessionStorage on mount
// Renders nothing
```

From src/lib/meta-pixel.ts:
```typescript
export function trackEvent(eventName: string, params?: Record<string, unknown>): void;
// Calls window.fbq("track", eventName, params)
```

<!-- Existing reusable component -->
From src/components/FadeUp.tsx:
```typescript
// Scroll-triggered animation wrapper using Framer Motion
// Wrap sections in <FadeUp> for entrance animations
```

<!-- Design system colors from globals.css -->
Sage: #2A6F5A (primary/CTA)
Ink: #0D0D0D (text)
Cream: #F5F0E8 (warm background)
Chalk: #FAFAF7 (light background)
Sage-deep: #1E5A46 (hover state)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MetaPixel, CalendlyButton components and wire into root layout with GA4</name>
  <files>src/components/MetaPixel.tsx, src/components/CalendlyButton.tsx, src/app/layout.tsx</files>
  <action>
1. Install new dependencies:
   ```bash
   npm install react-calendly @next/third-parties
   ```

2. Create `src/components/MetaPixel.tsx`:
   - "use client" component
   - Props: `pixelId?: string`
   - If no pixelId, return null (graceful degradation when env var not set)
   - Use `next/script` with `strategy="afterInteractive"` to load Facebook Pixel SDK
   - Initialize with `fbq('init', pixelId)` and fire `fbq('track', 'PageView')`
   - Include noscript fallback img tag
   - Declare global Window.fbq type

3. Create `src/components/CalendlyButton.tsx`:
   - "use client" component
   - Props: `url: string`, `text?: string` (defaults to "Book Your Free Strategy Call")
   - Use `PopupButton` from `react-calendly` for popup-on-click behavior (NOT inline embed, per user decision)
   - Use `useCalendlyEventListener` to listen for `onEventScheduled` and call `trackEvent("Schedule")` from `@/lib/meta-pixel`
   - Style the button to match SquareMind design: bg-sage, text-white, rounded-full, px-9 py-4, hover:bg-sage-deep hover:scale-[1.03] transition
   - CRITICAL SSR NOTE: react-calendly needs `document.body` for rootElement. Handle this with a `useState` + `useEffect` pattern to set rootElement only after mount, OR wrap the entire export with `dynamic(() => import(...), { ssr: false })`. Choose whichever approach avoids the "document is not defined" SSR error.

4. Update `src/app/layout.tsx` (root layout, already stripped of Navbar/Footer by Plan 01):
   - Import and render `<MetaPixel pixelId={process.env.NEXT_PUBLIC_META_PIXEL_ID} />` inside body, before {children}
   - Import `GoogleAnalytics` from `@next/third-parties/google` and render conditionally: `{process.env.NEXT_PUBLIC_GA4_ID && <GoogleAnalytics gaId={process.env.NEXT_PUBLIC_GA4_ID} />}` inside body, after {children}
   - Do NOT touch fonts, metadata, JSON-LD, or any other existing root layout content
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && test -f src/components/MetaPixel.tsx && test -f src/components/CalendlyButton.tsx && grep -q "MetaPixel" src/app/layout.tsx && grep -q "GoogleAnalytics" src/app/layout.tsx && grep -q "trackEvent.*Schedule" src/components/CalendlyButton.tsx && echo "TRACKING_OK" || echo "TRACKING_FAIL"</automated>
  </verify>
  <done>MetaPixel component loads FB Pixel SDK and fires PageView. CalendlyButton opens popup and fires Schedule event on booking. Root layout renders MetaPixel and GA4 site-wide. All components handle missing env vars gracefully (render nothing).</done>
</task>

<task type="auto">
  <name>Task 2: Build Tri-City investment guide landing page</name>
  <files>src/app/(landing)/invest/tri-city/page.tsx</files>
  <action>
Create the landing page at `src/app/(landing)/invest/tri-city/page.tsx`. This is a SERVER component (for metadata) that imports client components.

**Metadata:**
- Title: "Tri-City 2026 Investment Guide | SquareMind"
- Description: Advisory-toned meta description about Chandigarh, Mohali, Panchkula real estate investment opportunity
- OpenGraph image, URL, etc.

**Page structure (distraction-free, mobile-first, advisory/educational tone — NOT salesy):**

Section 1 — Hero:
- Strong headline about Tri-City 2026 investment opportunity (e.g., "The Smart Money Is Moving to Chandigarh Tri-City" or similar)
- 1-2 line subheading establishing SquareMind's advisory positioning
- Primary CTA: CalendlyButton ("Book Your Free Strategy Call")
- Secondary CTA: smooth scroll to form section
- SquareMind logo at top (link to homepage, small, subtle)

Section 2 — Why Tri-City:
- 3-4 key reasons Tri-City is a compelling investment (infrastructure development, IT corridor growth, airport expansion, government projects)
- Use data points and specific developments (advisory credibility)
- Can use icon cards or stat blocks

Section 3 — Trust Signals:
- Credentials: "Independent Advisory", "Zero Builder Commissions", "Data-Backed Analysis"
- Optional: consultation count, cities covered, or similar social proof stats
- Brief mention that SquareMind works with Homeland projects in the region (per user context: Meta ads promote Homeland builder projects)

Section 4 — How SquareMind Helps:
- 3-step process: "Get Expert Analysis → Choose the Right Property → Invest with Confidence"
- Brief value proposition for each step

Section 5 — Lead Form:
- Section heading: "Get Your Free Tri-City Investment Guide"
- Render `<TriCityForm />` component (from Plan 02)
- Render `<UTMCapture />` component (from Plan 02) — captures UTMs on page load

Section 6 — Calendly CTA:
- "Prefer to talk first?" or similar
- Render `<CalendlyButton url={process.env.NEXT_PUBLIC_CALENDLY_URL || ""} />`

Section 7 — Footer:
- Minimal landing page footer: SquareMind copyright, privacy link, disclaimer text
- NOT the full site footer

**Styling:**
- Mobile-first: single column layout, large tap targets, readable text sizes
- Use existing design system: Instrument Serif for headings, DM Sans for body, sage/ink/cream/chalk colors
- Use `<FadeUp>` wrapper on sections for scroll-triggered animations
- Ensure all interactive elements (buttons, form inputs) are at least 44px tap target on mobile

**ViewContent event:**
- Add a client component or useEffect at the top of the page that fires `trackEvent("ViewContent", { content_name: "Tri-City Investment Guide" })` on mount. This can be a small inline client component wrapper or part of UTMCapture.

**Important per user decisions:**
- NO main navigation bar (handled by (landing) layout)
- Advisory/educational tone, NOT salesy — build trust so they book a consultation
- Meta ads promote Homeland builder projects — page should feel like natural next step for someone who clicked a Homeland-related ad
- 70%+ traffic is mobile — every design decision prioritizes mobile
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && test -f "src/app/(landing)/invest/tri-city/page.tsx" && grep -q "TriCityForm" "src/app/(landing)/invest/tri-city/page.tsx" && grep -q "CalendlyButton" "src/app/(landing)/invest/tri-city/page.tsx" && grep -q "metadata" "src/app/(landing)/invest/tri-city/page.tsx" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Landing page renders at /invest/tri-city with educational Tri-City content, lead capture form, Calendly booking CTA, trust signals, and proper SEO metadata. No Navbar or Footer from main site. Mobile-optimized layout. ViewContent event fires on load.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete ad-to-lead pipeline</name>
  <what-built>Complete Phase 1 ad launch pipeline: Vercel-ready deployment, Tri-City landing page, lead capture form, Meta Pixel tracking, GA4, and Calendly booking</what-built>
  <how-to-verify>
1. Run `npm run dev` and visit http://localhost:3000
   - Verify existing pages (homepage, /about, /consultation) still render with Navbar and Footer
   - Verify no broken layouts or missing components

2. Visit http://localhost:3000/invest/tri-city
   - Verify NO Navbar or Footer appears (distraction-free layout)
   - Verify page has educational Tri-City content (not salesy)
   - Verify form has all 5 visible fields: Name, Email, Phone, Location dropdown, Budget dropdown
   - Open Chrome DevTools mobile view (iPhone 12, 390x844) — verify layout works on mobile

3. Submit the form with test data
   - Verify success message appears
   - Check Supabase dashboard — verify lead row appears in `leads` table with correct data
   - (If Meta Pixel ID is set) Check Meta Pixel Helper — verify Lead event fired

4. Click "Book Your Free Strategy Call" button
   - (If Calendly URL is set) Verify Calendly popup opens
   - (If not set) Verify no crash — button should gracefully handle missing URL

5. Visit http://localhost:3000/sitemap.xml — verify all URLs use squaremind.in domain
6. Visit http://localhost:3000/robots.txt — verify it references squaremind.in/sitemap.xml

7. Run `npx next build` — verify build succeeds with no errors
  </how-to-verify>
  <resume-signal>Type "approved" if everything works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm run dev` — existing pages render correctly with Navbar/Footer
2. `/invest/tri-city` renders without Navbar/Footer
3. Form submission reaches /api/leads and data appears in Supabase
4. Meta Pixel component renders in root layout (visible in page source)
5. GA4 component renders conditionally in root layout
6. CalendlyButton fires Schedule event on booking completion
7. `npx next build` succeeds
</verification>

<success_criteria>
- A visitor clicking a Meta ad lands on /invest/tri-city and sees a mobile-optimized, distraction-free landing page
- The visitor can submit a lead form (name, email, phone, location, budget) which saves to Supabase with UTM tracking
- Meta Pixel fires ViewContent on page load, Lead on form submit, Schedule on Calendly booking
- GA4 is installed site-wide
- "Book Your Free Strategy Call" opens Calendly popup
- All existing pages continue to work normally with Navbar/Footer
</success_criteria>

<output>
After completion, create `.planning/phases/01-ad-launch-pipeline/01-03-SUMMARY.md`
</output>
