---
phase: 07-content-quality-overhaul
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - content/posts/*.mdx
autonomous: false
requirements:
  - CQO-03
must_haves:
  truths:
    - "First batch of Investment Strategy posts are rewritten to 6000+ word comprehensive articles"
    - "Rewritten posts have TL;DR blocks, FAQ sections with 20+ questions, Sources with real URLs, and internal links"
    - "Blog builds and renders correctly after rewrite"
    - "User has verified quality of rewritten content"
  artifacts:
    - path: "content/posts/*.mdx"
      provides: "First batch of rewritten Investment Strategy posts"
  key_links:
    - from: "scripts/rewrite-batch.js"
      to: "content/posts/*.mdx"
      via: "Claude API rewrite pipeline"
      pattern: "rewrite-batch"
---

<objective>
Execute the first rewrite batch on Investment Strategy posts (highest priority -- drives consultation bookings), verify quality, then provide instructions for the user to continue running batches across remaining categories at their own pace (5-10 posts per day).

Purpose: Validate the rewrite pipeline works end-to-end on real posts before committing to all 301. Investment Strategy goes first because these posts drive the most business value.
Output: First batch of rewritten posts + verification + batch execution guide
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-content-quality-overhaul/07-CONTEXT.md
@.planning/phases/07-content-quality-overhaul/07-01-SUMMARY.md
@.planning/phases/07-content-quality-overhaul/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute first rewrite batch and validate pipeline</name>
  <files>content/posts/*.mdx</files>
  <action>
1. Check that ANTHROPIC_API_KEY is available:
   - Run `node scripts/rewrite-batch.js --status` to confirm pipeline state
   - If API key missing, create a checkpoint:human-action asking user to add it to .env.local

2. Run the first batch:
   ```
   node scripts/rewrite-batch.js --count 5 --category "Investment Strategy"
   ```

3. After batch completes, validate the output:
   - Check word counts of rewritten posts (should be 3000+ words each)
   - Verify TL;DR blocks exist (grep for `> **TL;DR`)
   - Verify FAQ sections exist (grep for `## Frequently Asked Questions`)
   - Verify Sources sections exist (grep for `## Sources`)
   - Verify internal links are present (grep for `](/insights/` and `](/tools/`)
   - Verify ToolCallout components are used (grep for `<ToolCallout`)
   - Check that frontmatter is preserved and unchanged (except readTime)

4. Run `npx next build` to verify the site still builds with rewritten content.

5. If any validation fails, inspect the rewrite-prompt.md and rewrite-batch.js for issues and fix before proceeding.

6. Run `node scripts/rewrite-batch.js --status` to show updated progress.
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && node scripts/rewrite-batch.js --status 2>&1 | head -20</automated>
  </verify>
  <done>5 Investment Strategy posts rewritten to 3000+ words each with TL;DR, FAQs, Sources, internal links. Build passes. Status shows 5 rewritten, 296 remaining.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify rewritten content quality</name>
  <files>content/posts/*.mdx</files>
  <action>Present the rewritten posts to user for quality review. User verifies content voice, structure, internal links, and ToolCallout rendering in the browser. If approved, user has CLI commands to continue rewriting remaining 296 posts independently.</action>
  <verify>
    <automated>echo "Checkpoint: awaiting human verification of content quality"</automated>
  </verify>
  <done>User confirms rewritten content meets SEOEngine.ai quality standard and approves the pipeline for remaining posts.</done>
  <what-built>First batch of 5 Investment Strategy posts rewritten via Claude API pipeline. Each post should now be 6000-8000 words with TL;DR block, 20+ FAQs, comparison tables, external citations, and 10-15 internal links including ToolCallout boxes.</what-built>
  <how-to-verify>
    1. Run `npx next dev` and visit http://localhost:3000/insights
    2. Open 2-3 of the rewritten Investment Strategy posts
    3. Check quality: Does the content read well? Is the SquareMind advisory voice correct?
    4. Check structure: TL;DR at top, H2/H3 sections, comparison table, FAQ section, Sources at bottom
    5. Check links: Click internal links (to other posts and tools) -- do they work?
    6. Check ToolCallout boxes: Are they styled correctly?
    7. Run `node scripts/rewrite-batch.js --status` to see progress

    If quality is good, you can continue rewriting at your own pace:
    ```
    node scripts/rewrite-batch.js --count 5 --category "Investment Strategy"
    node scripts/rewrite-batch.js --count 10  # follows priority order
    node scripts/rewrite-batch.js --status    # check progress anytime
    ```

    Category priority order: Investment Strategy (44) -> Tax & Legal (28) -> NRI Corner (29) -> City Guides (55) -> First-Time Buyers (35) -> Builder Analysis (25) -> Market Data (38) -> Due Diligence (29) -> Dark Truths (18)
  </how-to-verify>
  <resume-signal>Type "approved" if content quality meets expectations, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. At least 5 posts rewritten with word count > 3000
2. Rewritten posts contain TL;DR, FAQ, Sources sections
3. Internal links and ToolCallout components present
4. Site builds and renders correctly
5. User approves content quality
</verification>

<success_criteria>
- First batch validates the entire pipeline end-to-end
- User confirms content quality meets SEOEngine.ai standard
- CLI script is proven and ready for remaining 296 posts
- User has clear instructions to continue batch processing independently
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-quality-overhaul/07-03-SUMMARY.md`
</output>
