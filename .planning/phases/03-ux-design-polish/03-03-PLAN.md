---
phase: 03-ux-design-polish
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/SkeletonCard.tsx
  - src/components/animations/ScrollProgress.tsx
  - src/app/(main)/insights/[slug]/page.tsx
  - src/components/Navbar.tsx
  - src/app/(main)/page.tsx
  - src/app/(main)/consultation/page.tsx
  - src/app/(main)/about/page.tsx
  - src/app/(main)/research/page.tsx
  - src/app/globals.css
autonomous: false
requirements: [UX-06, UX-07, UX-09, UX-10]

must_haves:
  truths:
    - "Blog post pages show a thin progress bar at the top that fills as the user scrolls"
    - "Skeleton placeholder cards exist as a reusable component for future dynamic content"
    - "Navbar background transitions from transparent to frosted glass on scroll"
    - "Section padding is consistent across all pages (120px desktop, 80px mobile)"
    - "Typography scale follows a clear hierarchy with consistent tracking values"
    - "Key sections use gradient backgrounds for visual depth"
  artifacts:
    - path: "src/components/animations/ScrollProgress.tsx"
      provides: "Fixed progress bar showing read progress on blog posts"
      exports: ["ScrollProgress"]
    - path: "src/components/SkeletonCard.tsx"
      provides: "Reusable skeleton loading card with pulse animation"
      exports: ["default"]
  key_links:
    - from: "src/app/(main)/insights/[slug]/page.tsx"
      to: "src/components/animations/ScrollProgress.tsx"
      via: "import and render at top of blog post page"
      pattern: "ScrollProgress"
    - from: "src/components/Navbar.tsx"
      to: "glassmorphism classes"
      via: "enhanced backdrop-blur and bg-opacity values on scroll"
      pattern: "backdrop-blur.*bg-chalk"
---

<objective>
Add scroll progress indicator on blog posts, create skeleton screen components, refine the navbar glassmorphism, and audit/polish spacing, typography, and gradient backgrounds across all main pages.

Purpose: This is the polish pass -- the details that distinguish a professional site from a good one. Scroll progress keeps readers engaged, consistent spacing creates visual rhythm, and glassmorphism on the navbar adds the modern premium feel.
Output: Blog scroll progress bar, skeleton component, refined navbar glassmorphism, consistent spacing and gradients across all pages.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ux-design-polish/03-RESEARCH.md

@src/components/Navbar.tsx
@src/app/(main)/insights/[slug]/page.tsx
@src/app/globals.css

<interfaces>
<!-- Blog post page structure (server component with generateMetadata) -->
From src/app/(main)/insights/[slug]/page.tsx:
```typescript
// Server component -- exports generateMetadata, generateStaticParams
// Cannot add "use client" -- import client components instead
```

<!-- Navbar scroll state from Navbar.tsx -->
From src/components/Navbar.tsx:
```typescript
const [scrolled, setScrolled] = useState(false);
// scrolled ? "bg-chalk/95 shadow-[0_1px_0_rgba(0,0,0,0.06)]" : "bg-chalk/85"
// Already has backdrop-blur-[24px]
```

<!-- Design tokens from globals.css -->
From src/app/globals.css:
```css
/* Colors: ink, chalk, cream, sage, sage-deep, sage-light, sage-muted, warm, red, gold */
/* Section spacing: py-[120px] max-lg:py-20 */
/* Container: mx-auto max-w-[1200px] px-6 lg:px-14 */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScrollProgress and SkeletonCard, integrate into blog posts</name>
  <files>
    src/components/animations/ScrollProgress.tsx
    src/components/SkeletonCard.tsx
    src/app/(main)/insights/[slug]/page.tsx
  </files>
  <action>
    1. Create `src/components/animations/ScrollProgress.tsx`:
       - "use client" directive
       - Import { motion, useScroll, useSpring } from "framer-motion"
       - useScroll() for scrollYProgress (whole page progress)
       - useSpring(scrollYProgress, { stiffness: 100, damping: 30, restDelta: 0.001 }) for smooth tracking
       - Render: motion.div with className "fixed top-0 left-0 right-0 h-[3px] bg-sage origin-left z-[60]" and style={{ scaleX }}
       - Export ScrollProgress as named export

    2. Create `src/components/SkeletonCard.tsx`:
       - No "use client" needed (pure CSS animation via Tailwind animate-pulse)
       - Default export function SkeletonCard
       - Render a card skeleton matching the site's card pattern:
         - Outer: bg-cream rounded-[20px] p-9 animate-pulse
         - Tag placeholder: h-3 w-20 bg-cream-dark rounded mb-4
         - Title placeholder: h-5 w-full bg-cream-dark rounded mb-2
         - Subtitle placeholder: h-5 w-3/4 bg-cream-dark rounded mb-4
         - Meta placeholder: h-3 w-24 bg-cream-dark rounded
       - Also export a SkeletonLine variant for inline loading states:
         - Props: { width?: string; height?: string; className?: string }
         - Render: div with animate-pulse bg-cream-dark rounded, using provided width/height

    3. Modify `src/app/(main)/insights/[slug]/page.tsx`:
       - Import { ScrollProgress } from "@/components/animations/ScrollProgress"
       - Add `<ScrollProgress />` as the first element inside the return JSX (before the article content)
       - The page is a server component, but ScrollProgress is a client component -- this is fine (server can import client components)
       - Do NOT add "use client" to the page
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Blog post pages show a thin sage-colored progress bar fixed at the top that tracks scroll position with spring physics. SkeletonCard component exists for future use. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Glassmorphism navbar refinement and spacing/typography audit across all pages</name>
  <files>
    src/components/Navbar.tsx
    src/app/(main)/page.tsx
    src/app/(main)/consultation/page.tsx
    src/app/(main)/about/page.tsx
    src/app/(main)/research/page.tsx
    src/app/globals.css
  </files>
  <action>
    1. Enhance glassmorphism in `src/components/Navbar.tsx` (UX-10):
       - Update the scrolled/not-scrolled classes:
         - Not scrolled: "backdrop-blur-xl bg-chalk/60" (more transparent, more glass feel)
         - Scrolled: "backdrop-blur-2xl bg-chalk/80 shadow-[0_1px_0_rgba(0,0,0,0.06)]" (stronger blur, slight opacity increase)
       - This is a subtle refinement -- the navbar already has glassmorphism. We are adjusting opacity values for a more pronounced frosted-glass effect with more transparency when not scrolled.

    2. Spacing and typography audit across pages (UX-09):
       Read each page file and verify/fix consistent patterns:

       **Consistency rules to enforce:**
       - All page sections use py-[120px] max-lg:py-20 (or bg-cream/bg-ink sections)
       - All containers use mx-auto max-w-[1200px] px-6 lg:px-14
       - Section labels: text-[13px] font-semibold tracking-[0.08em] uppercase text-sage mb-4 block
       - Section headings: font-serif text-[clamp(36px,4.5vw,64px)] leading-[1.08] tracking-[-0.03em]
       - Section descriptions: text-[18px] text-gray-500 max-w-[560px] mt-4 leading-[1.65] tracking-[-0.01em]
       - Body text: text-[15px] tracking-[-0.01em] leading-relaxed

       For each page (consultation, about, research), read the file and make corrections where any of these patterns are inconsistent. Common issues:
       - Missing max-lg:py-20 on sections that have py-[120px]
       - Inconsistent tracking values on headings
       - Missing max-w-[560px] on description paragraphs
       - Section spacing that uses different values

       Do NOT change content, layout, or functionality. Only harmonize spacing, padding, tracking, and typography class values.

    3. Add gradient backgrounds to key sections (UX-10):
       - In `src/app/globals.css`, add utility classes if needed for reusable gradients:
         - No need if we use inline Tailwind gradient classes (preferred)
       - In homepage (already partially done in plan 01 -- verify and complement):
         - Ensure "How It Works" (bg-cream section) has a subtle warm gradient: bg-gradient-to-br from-cream to-cream-dark/30 (if this looks good with the existing cream background)
         - If plan 01 already added gradients to "Who We Serve" and "Final CTA", leave those alone
       - In consultation page: add a subtle gradient to the hero/header section if it has a colored background
  </action>
  <verify>
    <automated>cd /Users/sakshamsolanki/Desktop/SquareMind/website && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Navbar shows more pronounced frosted-glass effect (more transparent when not scrolled, stronger blur on scroll). Section padding, typography tracking, and container widths are consistent across homepage, consultation, about, and research pages. Gradient accents applied to appropriate sections. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all UX polish features</name>
  <files>none</files>
  <action>
    Human verifies all 10 UX features implemented across plans 01, 02, and 03. Run npm run dev and test each feature in browser.

    What was built:
    - Page transitions between routes (opacity fade)
    - Animated count-up stats on homepage
    - Testimonial carousel with autoplay, swipe, pause-on-hover
    - Mobile nav slide-in drawer with backdrop blur
    - Scroll progress bar on blog posts
    - Parallax hero section
    - Glassmorphism navbar refinement
    - Enhanced hover micro-interactions (shadow lift, focus rings)
    - Consistent spacing and typography across pages
    - Gradient backgrounds on key sections

    Verification steps:
    1. Run `npm run dev` and open http://localhost:3000
    2. Page transitions: Click between Home, About, Research, Insights -- each transition should fade smoothly
    3. Stats count-up: Scroll to stats bar -- numbers animate from 0. Text values appear instantly
    4. Testimonial carousel: Auto-advances every 5s. Hover to pause. Swipe on mobile viewport
    5. Mobile nav: Resize to mobile. Tap hamburger -- slides in from right with blur backdrop
    6. Blog scroll progress: Navigate to any blog post. Sage bar at top fills as you scroll
    7. Parallax: Homepage hero text moves at different rate than page scroll
    8. Glassmorphism: Navbar transitions from transparent to frosted glass on scroll
    9. Hover effects: Cards show shadow lift, buttons scale
    10. Reduced motion: Enable OS reduced motion -- animations should be minimized
  </action>
  <verify>Human visual inspection in browser</verify>
  <done>All 10 UX features confirmed working. User types "approved".</done>
</task>

</tasks>

<verification>
- `npx next build` passes with no errors
- All 10 UX requirements (UX-01 through UX-10) addressed
- ScrollProgress renders on blog post pages
- SkeletonCard component exists for future use
- Navbar glassmorphism is more pronounced
- Section spacing is consistent across main pages
</verification>

<success_criteria>
1. Blog post pages show scroll progress bar (sage colored, fixed top, spring physics)
2. SkeletonCard component exists and can be used for future dynamic content loading
3. Navbar shows frosted-glass effect that strengthens on scroll
4. Section padding is py-[120px] max-lg:py-20 consistently across all audited pages
5. Typography tracking values match the established convention
6. Human verification confirms all 10 UX features work as expected
</success_criteria>

<output>
After completion, create `.planning/phases/03-ux-design-polish/03-03-SUMMARY.md`
</output>
