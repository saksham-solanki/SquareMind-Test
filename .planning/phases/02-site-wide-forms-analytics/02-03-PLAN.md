---
phase: 02-site-wide-forms-analytics
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/NewsletterForm.tsx
  - src/app/(main)/page.tsx
  - src/app/(main)/insights/[slug]/page.tsx
autonomous: true
requirements: [NEWS-01, NEWS-02, LEAD-04]

must_haves:
  truths:
    - "Newsletter signup on homepage collects email and saves to Supabase with form_type='newsletter'"
    - "Newsletter signup on blog post pages collects email and saves to Supabase with form_type='newsletter'"
    - "Subscriber sees a success confirmation message after submitting their email"
    - "Newsletter form shows loading state during submission"
  artifacts:
    - path: "src/components/NewsletterForm.tsx"
      provides: "Reusable newsletter email capture component"
      exports: ["default"]
      contains: "form_type.*newsletter"
    - path: "src/app/(main)/page.tsx"
      provides: "Homepage with NewsletterForm replacing inline HTML form"
      contains: "NewsletterForm"
    - path: "src/app/(main)/insights/[slug]/page.tsx"
      provides: "Blog post page with NewsletterForm replacing inline HTML form"
      contains: "NewsletterForm"
  key_links:
    - from: "src/components/NewsletterForm.tsx"
      to: "/api/leads"
      via: "fetch POST with form_type='newsletter'"
      pattern: "form_type.*newsletter"
    - from: "src/app/(main)/page.tsx"
      to: "src/components/NewsletterForm.tsx"
      via: "import and render in newsletter section"
      pattern: "NewsletterForm"
    - from: "src/app/(main)/insights/[slug]/page.tsx"
      to: "src/components/NewsletterForm.tsx"
      via: "import and render in mid-article CTA"
      pattern: "NewsletterForm"
---

<objective>
Create a reusable NewsletterForm component that captures email to Supabase and replace the two inline dead forms (homepage newsletter section and blog post mid-article CTA) with it.

Purpose: Newsletter signups across the site actually capture emails instead of doing nothing. Subscribers see confirmation feedback.
Output: NewsletterForm component, updated homepage, updated blog post page.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-site-wide-forms-analytics/02-RESEARCH.md
@.planning/phases/02-site-wide-forms-analytics/02-01-SUMMARY.md

<interfaces>
<!-- Depends on Plan 01 having updated /api/leads to accept email-only forms -->

From src/app/api/leads/route.ts (after Plan 01):
```typescript
// Accepts form_type="newsletter" with email-only (no name/phone required)
// POST body: { email, form_type: "newsletter", source_page, ...utmParams }
```

From src/lib/utm.ts:
```typescript
export function getUTMParams(): Record<string, string>
```

From src/lib/meta-pixel.ts:
```typescript
export function trackEvent(eventName: string, params?: Record<string, string>): void
```

From src/lib/analytics.ts (created in Plan 02):
```typescript
export function trackFormSubmit(formType: string): void
```

From src/app/(main)/page.tsx (lines 282-298 -- newsletter section to replace):
```html
<!-- NEWSLETTER section contains bare <form> with email input and submit button -->
<!-- No handler, no action, completely dead -->
```

From src/app/(main)/insights/[slug]/page.tsx (lines 116-131 -- mid-article CTA to replace):
```html
<!-- Mid-article newsletter CTA with bare <form> -->
<!-- No handler, no action, completely dead -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NewsletterForm component</name>
  <files>src/components/NewsletterForm.tsx</files>
  <action>
    Create src/components/NewsletterForm.tsx ("use client"):

    Props: { className?: string } -- allows parent to pass styling context

    State: loading (boolean), submitted (boolean), error (string | null)

    handleSubmit:
    - Prevent default, set loading true, clear error
    - Get email from FormData
    - Import and call getUTMParams() from "@/lib/utm"
    - POST to /api/leads with { email, form_type: "newsletter", source_page: window.location.pathname, ...utmParams }
    - On success (res.ok): setSubmitted(true), call trackEvent("Lead", { content_name: "newsletter" }) from meta-pixel, call trackFormSubmit("newsletter") from analytics (import conditionally -- it may not exist yet if Plan 02 hasn't run, so wrap in try/catch or check typeof)
    - On error: parse error from response JSON, setError(message)
    - Finally: setLoading(false)

    Render:
    - If submitted: show success message "You're in! Check your inbox." with a green checkmark or similar. Style to match existing site aesthetic (bg-cream, text-ink, rounded-full inputs).
    - If not submitted: render form with email input (type="email", required, placeholder="your@email.com") and submit button ("Subscribe" or "Join the Brief")
    - Show error below form if error state is set
    - Use existing site styling: rounded-full inputs, px-5 py-3.5, border-gray-300, bg-white for input. Button matches site CTA style.
    - Apply className prop to outer wrapper for layout flexibility

    Important: Keep styling consistent with the existing inline forms being replaced (same input style, same general look). The homepage section has specific layout (flex-col sm:flex-row gap-2.5 max-w-[440px] mx-auto) -- this layout should be INSIDE the component since both locations use a similar inline layout.
  </action>
  <verify>
    <automated>test -f src/components/NewsletterForm.tsx && grep -q "form_type.*newsletter" src/components/NewsletterForm.tsx && grep -q "api/leads" src/components/NewsletterForm.tsx && grep -q "submitted" src/components/NewsletterForm.tsx</automated>
  </verify>
  <done>NewsletterForm component exists with email capture to /api/leads, loading state, success confirmation, error display, and Meta Pixel tracking</done>
</task>

<task type="auto">
  <name>Task 2: Replace inline dead forms with NewsletterForm on homepage and blog posts</name>
  <files>src/app/(main)/page.tsx, src/app/(main)/insights/[slug]/page.tsx</files>
  <action>
    1. src/app/(main)/page.tsx:
       - Import NewsletterForm from "@/components/NewsletterForm"
       - Find the NEWSLETTER section (around line 282-296)
       - Replace the bare `<form>` element (lines ~291-296) with `<NewsletterForm />`
       - Keep the surrounding section structure, heading text ("Join 2,000+ investors..."), and the "Also get The SquareMind Brief" subtext
       - The NewsletterForm component handles the input + button layout internally

    2. src/app/(main)/insights/[slug]/page.tsx:
       - Import NewsletterForm from "@/components/NewsletterForm"
       - Find the mid-article newsletter CTA (around line 116-131)
       - Replace the bare `<form>` element (lines ~126-131) with `<NewsletterForm />`
       - Keep the surrounding CTA wrapper, heading, and descriptive text
       - Note: This file is a Server Component that uses generateStaticParams. Since NewsletterForm is a client component, just import it normally -- Next.js handles the client/server boundary at the component level.
  </action>
  <verify>
    <automated>grep -q "NewsletterForm" src/app/\(main\)/page.tsx && grep -q "NewsletterForm" src/app/\(main\)/insights/\[slug\]/page.tsx && npx next build</automated>
  </verify>
  <done>Homepage newsletter section and blog post mid-article CTA both use NewsletterForm component, no dead forms remain, build passes</done>
</task>

</tasks>

<verification>
- `npx next build` passes
- No bare `<form>` elements without handlers remain in newsletter sections
- NewsletterForm component renders on both homepage and blog post pages
- Submitting the newsletter form posts to /api/leads with form_type="newsletter"
</verification>

<success_criteria>
- Newsletter signups on homepage and blog posts save email to Supabase
- Users see success confirmation after subscribing
- No dead forms remain on the site
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-wide-forms-analytics/02-03-SUMMARY.md`
</output>
