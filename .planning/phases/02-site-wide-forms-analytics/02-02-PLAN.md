---
phase: 02-site-wide-forms-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analytics.ts
  - src/components/Hotjar.tsx
  - src/components/CalendlyButton.tsx
  - src/components/WhatsAppFloat.tsx
  - src/app/layout.tsx
  - src/app/(main)/consultation/page.tsx
autonomous: true
requirements: [BOOK-01, BOOK-02, BOOK-04, ANAL-01, ANAL-04, ANAL-05, ANAL-06]
user_setup:
  - service: hotjar
    why: "Session recording on key pages"
    env_vars:
      - name: NEXT_PUBLIC_HOTJAR_ID
        source: "Hotjar Dashboard -> Sites & Organizations -> Site ID"

must_haves:
  truths:
    - "GA4 custom events (form_submit, calendly_open, whatsapp_click) fire when corresponding actions occur"
    - "Hotjar script loads on all pages when NEXT_PUBLIC_HOTJAR_ID is set"
    - "Calendly popup is accessible on the consultation page"
    - "WhatsApp float uses configurable phone number from env var"
    - "CalendlyButton fires calendly_open GA4 event when popup opens"
    - "WhatsAppFloat fires whatsapp_click GA4 event on click"
  artifacts:
    - path: "src/lib/analytics.ts"
      provides: "GA4 custom event helpers"
      exports: ["trackFormSubmit", "trackCalendlyOpen", "trackWhatsAppClick"]
    - path: "src/components/Hotjar.tsx"
      provides: "Hotjar script loader component"
      exports: ["default"]
    - path: "src/components/CalendlyButton.tsx"
      provides: "Updated CalendlyButton with GA4 tracking"
      contains: "trackCalendlyOpen"
    - path: "src/components/WhatsAppFloat.tsx"
      provides: "Updated WhatsAppFloat with env var + GA4 tracking"
      contains: "NEXT_PUBLIC_WHATSAPP_NUMBER"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/Hotjar.tsx"
      via: "Hotjar component in root layout"
      pattern: "Hotjar"
    - from: "src/components/CalendlyButton.tsx"
      to: "src/lib/analytics.ts"
      via: "trackCalendlyOpen on popup open"
      pattern: "trackCalendlyOpen"
    - from: "src/components/WhatsAppFloat.tsx"
      to: "src/lib/analytics.ts"
      via: "trackWhatsAppClick on link click"
      pattern: "trackWhatsAppClick"
---

<objective>
Add GA4 custom event tracking helpers, Hotjar session recording, CalendlyButton on the consultation page, update WhatsApp to use env var, and wire GA4 events into CalendlyButton and WhatsAppFloat.

Purpose: Complete analytics coverage (GA4 custom events, Hotjar recordings) and ensure Calendly booking is accessible from the consultation page, not just the landing page.
Output: GA4 analytics helpers, Hotjar component, updated CalendlyButton/WhatsAppFloat with event tracking, consultation page with Calendly CTA.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-site-wide-forms-analytics/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/CalendlyButton.tsx (CURRENT):
```typescript
import { PopupButton, useCalendlyEventListener } from "react-calendly";
export default function CalendlyButton({ url, text = "Book Your Free Strategy Call" }: { url: string; text?: string })
// Uses useState/useEffect for SSR-safe rootElement
// Already has useCalendlyEventListener for Schedule Meta Pixel event
```

From src/components/WhatsAppFloat.tsx (CURRENT):
```typescript
export default function WhatsAppFloat()
// Hardcoded wa.me/919876543210 -- needs env var
```

From src/app/layout.tsx (CURRENT):
```typescript
import MetaPixel from "@/components/MetaPixel";
import { GoogleAnalytics } from "@next/third-parties/google";
// MetaPixel and GoogleAnalytics already in root layout
```

From src/app/(main)/consultation/page.tsx (CURRENT):
```typescript
import ConsultForm from "@/components/ConsultForm";
// Has ConsultForm but no CalendlyButton
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics helpers and Hotjar component</name>
  <files>src/lib/analytics.ts, src/components/Hotjar.tsx</files>
  <action>
    1. Create src/lib/analytics.ts ("use client" at top):
       - Import { sendGAEvent } from "@next/third-parties/google"
       - export function trackFormSubmit(formType: string) -- sends event "form_submit" with { form_type: formType }
       - export function trackCalendlyOpen() -- sends event "calendly_open"
       - export function trackWhatsAppClick() -- sends event "whatsapp_click"
       - Add a safety wrapper: check if window is defined before calling sendGAEvent (SSR safety)

    2. Create src/components/Hotjar.tsx ("use client"):
       - Props: { siteId?: string }
       - If !siteId, return null (graceful degradation, same pattern as MetaPixel)
       - Use next/script with strategy="afterInteractive"
       - Embed the standard Hotjar snippet (hjid: siteId, hjsv: 6)
       - See Research doc Pattern 4 for exact snippet
  </action>
  <verify>
    <automated>test -f src/lib/analytics.ts && test -f src/components/Hotjar.tsx && grep -q "trackFormSubmit" src/lib/analytics.ts && grep -q "trackCalendlyOpen" src/lib/analytics.ts && grep -q "trackWhatsAppClick" src/lib/analytics.ts</automated>
  </verify>
  <done>GA4 event helpers exist with three exported functions, Hotjar component exists with env-var gating</done>
</task>

<task type="auto">
  <name>Task 2: Wire analytics into CalendlyButton, WhatsAppFloat, layout, and consultation page</name>
  <files>src/components/CalendlyButton.tsx, src/components/WhatsAppFloat.tsx, src/app/layout.tsx, src/app/(main)/consultation/page.tsx</files>
  <action>
    1. CalendlyButton.tsx:
       - Import { trackCalendlyOpen } from "@/lib/analytics"
       - Add onProfilePageViewed callback to existing useCalendlyEventListener: call trackCalendlyOpen()
       - Keep existing Meta Pixel Schedule event tracking (onEventScheduled)
       - This covers ANAL-04 (Schedule event already fires) and ANAL-06 (calendly_open)

    2. WhatsAppFloat.tsx:
       - Import { trackWhatsAppClick } from "@/lib/analytics"
       - Replace hardcoded "919876543210" with: process.env.NEXT_PUBLIC_WHATSAPP_NUMBER || "919876543210"
       - Add onClick handler that calls trackWhatsAppClick()
       - Keep existing styling and aria-label

    3. src/app/layout.tsx:
       - Import Hotjar from "@/components/Hotjar"
       - Add <Hotjar siteId={process.env.NEXT_PUBLIC_HOTJAR_ID} /> next to existing MetaPixel component
       - ANAL-01 (GA4 site-wide) is already satisfied by GoogleAnalytics in layout -- no change needed

    4. src/app/(main)/consultation/page.tsx:
       - Import CalendlyButton from "@/components/CalendlyButton"
       - Add CalendlyButton below or alongside the ConsultForm section
       - Use process.env.NEXT_PUBLIC_CALENDLY_URL for the url prop
       - Only render if calendlyUrl is truthy (same pattern as landing page)
       - This covers BOOK-01 (CTAs open Calendly) and BOOK-02 (consultation page has Calendly)
  </action>
  <verify>
    <automated>grep -q "trackCalendlyOpen" src/components/CalendlyButton.tsx && grep -q "NEXT_PUBLIC_WHATSAPP_NUMBER" src/components/WhatsAppFloat.tsx && grep -q "Hotjar" src/app/layout.tsx && grep -q "CalendlyButton" src/app/(main)/consultation/page.tsx && npx next build</automated>
  </verify>
  <done>CalendlyButton fires calendly_open GA4 event, WhatsAppFloat uses env var and fires whatsapp_click GA4 event, Hotjar loads site-wide, consultation page has Calendly CTA, build passes</done>
</task>

</tasks>

<verification>
- `npx next build` passes with no type errors
- CalendlyButton tracks both Meta Pixel Schedule and GA4 calendly_open events
- WhatsAppFloat reads phone number from env var
- Hotjar component in root layout
- Consultation page has both ConsultForm and CalendlyButton
</verification>

<success_criteria>
- GA4 custom events (form_submit, calendly_open, whatsapp_click) have helper functions
- Hotjar loads site-wide when NEXT_PUBLIC_HOTJAR_ID is set
- CalendlyButton accessible on consultation page (not just landing page)
- WhatsApp uses real number from env var
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-wide-forms-analytics/02-02-SUMMARY.md`
</output>
