---
phase: 02-site-wide-forms-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/leads/route.ts
  - src/lib/resend.ts
  - src/components/HeroForm.tsx
  - src/components/ConsultForm.tsx
  - src/components/DownloadGate.tsx
  - .env.example
autonomous: true
requirements: [LEAD-01, LEAD-02, LEAD-03, LEAD-04, LEAD-07]
user_setup:
  - service: resend
    why: "Transactional email notifications on form submissions"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
      - name: RESEND_NOTIFICATION_TO
        source: "Comma-separated team email addresses (e.g. saksham@squaremind.in)"
    dashboard_config:
      - task: "Verify squaremind.in domain (add DNS records: SPF, DKIM)"
        location: "Resend Dashboard -> Domains -> Add Domain -> squaremind.in"

must_haves:
  truths:
    - "HeroForm submission saves lead data to Supabase with form_type='hero' and UTM params"
    - "ConsultForm submission saves lead data to Supabase with form_type='consultation' and UTM params"
    - "DownloadGate submission saves email to Supabase with form_type='download_gate'"
    - "Team receives email notification within 60 seconds of any form submission"
    - "Email-only forms (newsletter, download_gate) are accepted by API without name/phone"
  artifacts:
    - path: "src/lib/resend.ts"
      provides: "Lazy-init Resend client"
      exports: ["getResend"]
    - path: "src/app/api/leads/route.ts"
      provides: "Updated API with email-only validation + Resend notification"
      contains: "resend.emails.send"
    - path: "src/components/HeroForm.tsx"
      provides: "HeroForm wired to /api/leads"
      contains: "fetch.*api/leads"
    - path: "src/components/ConsultForm.tsx"
      provides: "ConsultForm wired to /api/leads"
      contains: "fetch.*api/leads"
    - path: "src/components/DownloadGate.tsx"
      provides: "DownloadGate wired to /api/leads"
      contains: "fetch.*api/leads"
  key_links:
    - from: "src/components/HeroForm.tsx"
      to: "/api/leads"
      via: "fetch POST with form_type='hero'"
      pattern: "form_type.*hero"
    - from: "src/components/ConsultForm.tsx"
      to: "/api/leads"
      via: "fetch POST with form_type='consultation'"
      pattern: "form_type.*consultation"
    - from: "src/app/api/leads/route.ts"
      to: "src/lib/resend.ts"
      via: "getResend() call after Supabase insert"
      pattern: "getResend"
---

<objective>
Wire all existing form components (HeroForm, ConsultForm, DownloadGate) to the /api/leads endpoint with correct form_type and UTM parameters, update API validation to support email-only forms, add Resend email notification on every submission, and create the Resend client library.

Purpose: Every form on the site captures leads to Supabase instead of faking success with setTimeout. Team gets instant email alerts.
Output: Three wired form components, updated API route, Resend client library.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-site-wide-forms-analytics/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/app/api/leads/route.ts (CURRENT - will be modified):
```typescript
import { getSupabaseAdmin } from "@/lib/supabase-server";
const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export async function POST(request: Request)
// Currently requires name, email, phone -- must be updated for email-only forms
```

From src/lib/utm.ts:
```typescript
export function getUTMParams(): Record<string, string>
// Returns { utm_source, utm_medium, utm_campaign, utm_content, utm_term } from sessionStorage
```

From src/lib/meta-pixel.ts:
```typescript
export function trackEvent(eventName: string, params?: Record<string, string>): void
// Calls fbq("track", eventName, params) if available
```

From src/lib/supabase-server.ts:
```typescript
export function getSupabaseAdmin(): SupabaseClient
// Lazy-init server-side Supabase with service role key
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Resend client and update /api/leads with email-only validation + notification</name>
  <files>src/lib/resend.ts, src/app/api/leads/route.ts, .env.example</files>
  <action>
    1. Create src/lib/resend.ts with lazy-init pattern (same as supabase-server.ts):
       - Import { Resend } from "resend"
       - let _resend: Resend | null = null
       - export function getResend(): Resend -- throws if RESEND_API_KEY missing
       - Cache instance in _resend

    2. Update src/app/api/leads/route.ts:
       - Add form_type-aware validation: define emailOnlyFormTypes = ["newsletter", "download_gate"]
       - If form_type is in emailOnlyFormTypes, only require email
       - Otherwise require name, email, phone (existing behavior)
       - After successful Supabase insert, add try/catch block calling getResend()
       - Send notification email: from "SquareMind Leads <leads@squaremind.in>", to process.env.RESEND_NOTIFICATION_TO (split by comma), subject "New Lead: {name || email} ({form_type})"
       - HTML body with all lead details (name, email, phone, source_page, utm_source, form_type)
       - If Resend fails, console.error but still return { success: true } -- lead is already saved
       - If RESEND_API_KEY is not set, skip email silently (don't throw)

    3. Add to .env.example: RESEND_API_KEY, RESEND_NOTIFICATION_TO, NEXT_PUBLIC_HOTJAR_ID, NEXT_PUBLIC_WHATSAPP_NUMBER
  </action>
  <verify>
    <automated>grep -q "emailOnlyFormTypes" src/app/api/leads/route.ts && test -f src/lib/resend.ts && grep -q "getResend" src/lib/resend.ts && grep -q "RESEND_API_KEY" .env.example</automated>
  </verify>
  <done>API route accepts email-only submissions for newsletter/download_gate form types, sends Resend notification after Supabase insert (gracefully skips if no API key), Resend client follows lazy-init pattern</done>
</task>

<task type="auto">
  <name>Task 2: Wire HeroForm, ConsultForm, and DownloadGate to /api/leads</name>
  <files>src/components/HeroForm.tsx, src/components/ConsultForm.tsx, src/components/DownloadGate.tsx</files>
  <action>
    1. HeroForm.tsx: Replace the setTimeout fake submission with real fetch to /api/leads:
       - Import { getUTMParams } from "@/lib/utm" and { trackEvent } from "@/lib/meta-pixel"
       - In handleSubmit: collect name, email, phone from FormData
       - POST to /api/leads with { name, email, phone, source_page: window.location.pathname, form_type: "hero", ...getUTMParams() }
       - On success: setSubmitted(true), trackEvent("Lead", { content_name: "hero_form" })
       - On error: show error message from response
       - Keep existing loading/submitted states and UI

    2. ConsultForm.tsx: Same pattern as HeroForm:
       - Import UTM and Meta Pixel helpers
       - Collect name, email, phone from form fields (ConsultForm uses custom FormField components -- read field names from JSX)
       - POST to /api/leads with form_type: "consultation"
       - Track Lead event with content_name: "consult_form"
       - Keep existing UI structure

    3. DownloadGate.tsx: Wire to /api/leads as email-only form:
       - Import { getUTMParams } from "@/lib/utm" and { trackEvent } from "@/lib/meta-pixel"
       - In handleSubmit: collect email from form
       - POST to /api/leads with { email, source_page: window.location.pathname, form_type: "download_gate", ...getUTMParams() }
       - On success: setSubmitted(true), trackEvent("Lead", { content_name: "download_gate" })
       - Keep existing modal/gate UI behavior

    Important: All three components are "use client" components. Use window.location.pathname for source_page. Do NOT add name/phone fields to DownloadGate -- it is email-only by design.
  </action>
  <verify>
    <automated>grep -q 'form_type.*hero' src/components/HeroForm.tsx && grep -q 'form_type.*consultation' src/components/ConsultForm.tsx && grep -q 'form_type.*download_gate' src/components/DownloadGate.tsx && grep -q 'api/leads' src/components/HeroForm.tsx && npx next build</automated>
  </verify>
  <done>All three forms POST to /api/leads with correct form_type and UTM params, fire Meta Pixel Lead events on success, show loading/success/error states, build passes</done>
</task>

</tasks>

<verification>
- `npx next build` passes with no type errors
- grep confirms all three form components reference /api/leads and their respective form_type values
- src/lib/resend.ts exists with getResend export
- /api/leads route handles both full-field and email-only form types
</verification>

<success_criteria>
- HeroForm, ConsultForm, DownloadGate all submit to /api/leads (no more setTimeout fakes)
- API route accepts email-only submissions for newsletter/download_gate
- Resend notification fires after every lead (gracefully skips if not configured)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-site-wide-forms-analytics/02-01-SUMMARY.md`
</output>
