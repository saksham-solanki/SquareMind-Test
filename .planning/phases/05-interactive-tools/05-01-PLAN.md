---
phase: 05-interactive-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/stamp-duty-rates.ts
  - src/data/rera-portals.ts
  - src/data/nri-tax-data.ts
  - src/data/city-benchmarks.ts
  - src/data/tool-registry.ts
  - src/components/calculators/shared/CalcField.tsx
  - src/components/calculators/shared/ResultRow.tsx
  - src/components/calculators/shared/VerdictBox.tsx
  - src/components/calculators/shared/ToolPageWrapper.tsx
  - src/app/(main)/tools/page.tsx
autonomous: true
requirements:
  - TOOL-09
  - TOOL-10

must_haves:
  truths:
    - "Tools listing page shows all 8 tools as linked cards instead of inline calculators"
    - "Each tool card links to /tools/{slug}"
    - "Shared UI components exist for consistent calculator styling"
    - "Data layer files exist with typed Indian real estate data"
  artifacts:
    - path: "src/data/tool-registry.ts"
      provides: "Tool metadata array for listing page and navigation"
      exports: ["TOOLS", "ToolMeta"]
    - path: "src/data/stamp-duty-rates.ts"
      provides: "State-wise stamp duty rates with gender-based pricing"
      exports: ["STAMP_DUTY_RATES", "StampDutyRate"]
    - path: "src/data/city-benchmarks.ts"
      provides: "Rental yield averages by Indian city"
      exports: ["CITY_BENCHMARKS"]
    - path: "src/data/rera-portals.ts"
      provides: "State RERA portal URLs"
      exports: ["RERA_PORTALS"]
    - path: "src/data/nri-tax-data.ts"
      provides: "NRI tax profiles by country and Indian tax slabs"
      exports: ["NRI_TAX_PROFILES", "INDIAN_TAX_SLABS"]
    - path: "src/components/calculators/shared/ToolPageWrapper.tsx"
      provides: "Shared layout wrapper for individual tool pages"
    - path: "src/app/(main)/tools/page.tsx"
      provides: "Refactored listing page with tool cards linking to individual pages"
  key_links:
    - from: "src/app/(main)/tools/page.tsx"
      to: "src/data/tool-registry.ts"
      via: "import TOOLS array"
      pattern: "import.*tool-registry"
---

<objective>
Create the shared data layer, reusable UI components, and refactor the tools listing page from inline calculators to linked tool cards.

Purpose: Every tool page (Plans 02-03) depends on shared data files, shared UI components, and the tool registry. Building these first prevents duplication and ensures consistency.
Output: Data files (stamp duty, RERA portals, NRI tax, city benchmarks, tool registry), shared calculator UI components, and a refactored /tools listing page.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-interactive-tools/05-RESEARCH.md

@src/app/(main)/tools/page.tsx
@src/app/(main)/tools/layout.tsx
@src/components/calculators/RentalYieldCalc.tsx
@src/components/calculators/BuyVsRentCalc.tsx
@src/components/calculators/TotalCostCalc.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create data layer files and shared UI components</name>
  <files>
    src/data/stamp-duty-rates.ts,
    src/data/rera-portals.ts,
    src/data/nri-tax-data.ts,
    src/data/city-benchmarks.ts,
    src/data/tool-registry.ts,
    src/components/calculators/shared/CalcField.tsx,
    src/components/calculators/shared/ResultRow.tsx,
    src/components/calculators/shared/VerdictBox.tsx,
    src/components/calculators/shared/ToolPageWrapper.tsx
  </files>
  <action>
    Create `src/data/` directory and the following typed TypeScript data files:

    1. **stamp-duty-rates.ts**: Export `StampDutyRate` interface (`state`, `code`, `male` %, `female` %, `registration` %, `regCap?`, `notes?`) and `STAMP_DUTY_RATES` array covering all 28 states + 8 UTs. Use the rates from RESEARCH.md data reference section. Key rates: Maharashtra 6-7%/5-6%, Karnataka 5%/3%, Delhi 6%/4%, Tamil Nadu 8%/7%, Telangana 5%/3%, UP 7%/4%, Punjab 6%/3%, Rajasthan 5%/3%, Gujarat 5%/3%, Haryana 6%/4%, West Bengal 6%/4%, Kerala 8%/6%, MP 7.5%/5.5%. For lesser-known states/UTs use 5%/3%/1% as conservative defaults with notes.

    2. **rera-portals.ts**: Export `RERAPortal` interface (`state`, `code`, `portalUrl`, `searchTip?`) and `RERA_PORTALS` array for ~22 states that have functional RERA portals. Use URLs from RESEARCH.md. Include a `searchTip` string explaining how to search on each major portal.

    3. **nri-tax-data.ts**: Export `CountryTaxProfile` interface and `NRI_TAX_PROFILES` array for 7 countries (US, UK, Canada, UAE, Singapore, Australia, India-Resident). Include `hasDTAA`, `tdsOnRental` rate, `capitalGainsRelief`, `notes`. Also export `INDIAN_TAX_SLABS` for NRI income tax brackets (FY 2025-26) and constants: `LTCG_RATE = 12.5`, `STCG_TDS = 30`, `CESS_RATE = 4`, surcharge brackets.

    4. **city-benchmarks.ts**: Export `CityBenchmark` interface (`city`, `avgGrossYield`, `yieldRange: [number, number]`, `avgPricePerSqft?`) and `CITY_BENCHMARKS` array for 8 major cities from RESEARCH.md (Bangalore, Mumbai, Pune, Hyderabad, Chennai, Delhi NCR, Chandigarh Tri-City, Ahmedabad).

    5. **tool-registry.ts**: Export `ToolMeta` interface (`slug`, `title`, `description`, `icon` as lucide icon name string, `status: "live"`, `category: "calculator" | "verifier" | "analyzer"`) and `TOOLS` array with all 8 tools: rental-yield, buy-vs-rent, total-cost, investment-scorecard, rera-verifier, nri-tax-calculator, emi-calculator, stamp-duty-calculator. All status "live". Use lucide icon names: TrendingUp, Scale, Calculator, Award, ShieldCheck, Globe, Landmark, Stamp.

    Then create `src/components/calculators/shared/` directory with:

    6. **CalcField.tsx**: Extract and generalize the CalcField/Field pattern from existing calculators. "use client" component. Props: `label`, `value`, `onChange`, `hint?`, `min?`, `max?`, `step?`, `type?: "number" | "select"`, `options?: {value: string, label: string}[]`. Match existing styling: `px-[18px] py-3.5 border-[1.5px] border-gray-300 rounded-[12px] text-[15px] bg-white text-ink focus:outline-none focus:border-ink focus:shadow-[0_0_0_3px_rgba(13,13,13,0.06)]`.

    7. **ResultRow.tsx**: Extract the Row component pattern. Props: `label`, `value`, `highlight?` (boolean for bold styling). Match existing: `py-2 text-[14px] text-gray-500`.

    8. **VerdictBox.tsx**: Reusable verdict display. Props: `variant: "positive" | "neutral" | "warning"`, `children` (ReactNode). Maps to existing color patterns: positive = `bg-sage-light text-sage-deep`, neutral = `bg-cream text-gray-600`, warning = `bg-[#FFF3E0] text-[#E65100]`.

    9. **ToolPageWrapper.tsx**: Server-compatible wrapper (no "use client"). Props: `title`, `description`, `children`. Renders: breadcrumb (Home / Tools / {title}), FadeUp-wrapped section with consistent spacing matching existing tools page (`py-[120px] max-lg:py-20`, `max-w-[1200px] px-6 lg:px-14`), plus a bottom CTA section linking to /consultation (reuse the dark CTA card from existing tools page). Import FadeUp and Link.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 5 data files export typed arrays. All 4 shared components exist with proper TypeScript types. No type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor tools listing page to card-based layout with links</name>
  <files>
    src/app/(main)/tools/page.tsx,
    src/app/(main)/tools/layout.tsx
  </files>
  <action>
    Rewrite `src/app/(main)/tools/page.tsx` to be a Server Component (remove "use client"):

    1. Import `TOOLS` from `@/data/tool-registry` and dynamically import the icon from lucide-react for each tool (or use a simple icon map component).

    2. Replace the current inline calculator rendering with a grid of tool cards. Each card:
       - Links to `/tools/${tool.slug}` via Next.js Link
       - Shows the lucide icon (rendered from the icon name string -- create a small `ToolIcon` helper that maps icon name to component)
       - Shows tool.title and tool.description
       - Styled matching the existing "More tools" card pattern: `bg-cream rounded-[20px] p-10 max-lg:p-7 flex gap-6 items-start hover:-translate-y-1 hover:shadow-[0_20px_60px_rgba(0,0,0,0.06)] transition-all duration-400`
       - Add a small arrow or "Try now" indicator

    3. Keep the page header (FadeUp with "Free Tools" tag, heading, subtitle) and bottom CTA section identical to current design.

    4. Grid layout: `grid grid-cols-1 md:grid-cols-2 gap-6 mt-10`

    5. Update `layout.tsx` metadata description to mention all 8 tools.

    Do NOT remove the existing calculator component files -- they will be used by individual route pages in Plans 02-03.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>The /tools page renders 8 tool cards linking to individual tool URLs. No inline calculators on the listing page. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- All data files importable and properly typed
- /tools page shows 8 tool cards (not inline calculators)
- Each card links to /tools/{slug}
</verification>

<success_criteria>
Data layer and shared components provide a foundation for all tool pages. The /tools listing page shows cards linking to individual tool URLs. Build passes cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/05-interactive-tools/05-01-SUMMARY.md`
</output>
