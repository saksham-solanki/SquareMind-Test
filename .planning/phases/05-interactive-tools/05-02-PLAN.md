---
phase: 05-interactive-tools
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/calculators/RentalYieldCalc.tsx
  - src/components/calculators/BuyVsRentCalc.tsx
  - src/components/calculators/TotalCostCalc.tsx
  - src/components/calculators/EMICalculator.tsx
  - src/components/calculators/StampDutyCalc.tsx
  - src/app/(main)/tools/rental-yield/page.tsx
  - src/app/(main)/tools/buy-vs-rent/page.tsx
  - src/app/(main)/tools/total-cost/page.tsx
  - src/app/(main)/tools/emi-calculator/page.tsx
  - src/app/(main)/tools/stamp-duty-calculator/page.tsx
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-07
  - TOOL-08
  - TOOL-09

must_haves:
  truths:
    - "Rental Yield Calculator shows city benchmark comparisons and allows adjustable tax rate"
    - "Buy vs Rent Calculator has user-adjustable rent inflation and equity return assumptions"
    - "Total Cost Calculator uses comprehensive state-wise stamp duty rates with gender selection"
    - "EMI Calculator produces correct monthly EMI and shows amortization schedule"
    - "Stamp Duty Calculator shows correct state-wise rates with gender-based differences"
    - "Each of these 5 tools is accessible at its own /tools/{slug} URL"
  artifacts:
    - path: "src/app/(main)/tools/rental-yield/page.tsx"
      provides: "SEO-optimized route page for rental yield tool"
      exports: ["metadata", "default"]
    - path: "src/app/(main)/tools/emi-calculator/page.tsx"
      provides: "SEO-optimized route page for EMI calculator"
      exports: ["metadata", "default"]
    - path: "src/app/(main)/tools/stamp-duty-calculator/page.tsx"
      provides: "SEO-optimized route page for stamp duty tool"
      exports: ["metadata", "default"]
    - path: "src/components/calculators/EMICalculator.tsx"
      provides: "EMI calculation with amortization schedule"
    - path: "src/components/calculators/StampDutyCalc.tsx"
      provides: "State-wise stamp duty calculator with gender rates"
  key_links:
    - from: "src/components/calculators/RentalYieldCalc.tsx"
      to: "src/data/city-benchmarks.ts"
      via: "import CITY_BENCHMARKS"
      pattern: "import.*city-benchmarks"
    - from: "src/components/calculators/TotalCostCalc.tsx"
      to: "src/data/stamp-duty-rates.ts"
      via: "import STAMP_DUTY_RATES"
      pattern: "import.*stamp-duty-rates"
    - from: "src/components/calculators/StampDutyCalc.tsx"
      to: "src/data/stamp-duty-rates.ts"
      via: "import STAMP_DUTY_RATES"
      pattern: "import.*stamp-duty-rates"
---

<objective>
Fix the 3 existing calculators with improved data and UX, build 2 new financial calculators (EMI, Stamp Duty), and create individual SEO route pages for all 5.

Purpose: These 5 tools are the core financial calculators -- they share the stamp duty data layer and form the backbone of the tools section.
Output: 5 working calculator tools, each at its own URL with unique SEO metadata.
</objective>

<execution_context>
@/Users/sakshamsolanki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sakshamsolanki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-interactive-tools/05-RESEARCH.md
@.planning/phases/05-interactive-tools/05-01-SUMMARY.md

@src/components/calculators/RentalYieldCalc.tsx
@src/components/calculators/BuyVsRentCalc.tsx
@src/components/calculators/TotalCostCalc.tsx

<interfaces>
<!-- From Plan 01 outputs -- executor needs these contracts -->

From src/data/stamp-duty-rates.ts:
```typescript
export interface StampDutyRate {
  state: string;
  code: string;
  male: number;
  female: number;
  registration: number;
  regCap?: number;
  notes?: string;
}
export const STAMP_DUTY_RATES: StampDutyRate[];
```

From src/data/city-benchmarks.ts:
```typescript
export interface CityBenchmark {
  city: string;
  avgGrossYield: number;
  yieldRange: [number, number];
  avgPricePerSqft?: number;
}
export const CITY_BENCHMARKS: CityBenchmark[];
```

From src/components/calculators/shared/CalcField.tsx:
```typescript
// "use client" - reusable form field with number input or select
Props: { label, value, onChange, hint?, min?, max?, step?, type?, options? }
```

From src/components/calculators/shared/ResultRow.tsx:
```typescript
Props: { label, value, highlight? }
```

From src/components/calculators/shared/VerdictBox.tsx:
```typescript
Props: { variant: "positive" | "neutral" | "warning", children }
```

From src/components/calculators/shared/ToolPageWrapper.tsx:
```typescript
// Server-compatible wrapper with breadcrumb, section spacing, bottom CTA
Props: { title, description, children }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 3 existing calculators and create their route pages</name>
  <files>
    src/components/calculators/RentalYieldCalc.tsx,
    src/components/calculators/BuyVsRentCalc.tsx,
    src/components/calculators/TotalCostCalc.tsx,
    src/app/(main)/tools/rental-yield/page.tsx,
    src/app/(main)/tools/buy-vs-rent/page.tsx,
    src/app/(main)/tools/total-cost/page.tsx
  </files>
  <action>
    **RentalYieldCalc.tsx improvements (TOOL-01):**
    - Replace hardcoded 30% tax with a CalcField for tax bracket (default 30%, options: 0%, 5%, 20%, 30%)
    - Import `CITY_BENCHMARKS` from `@/data/city-benchmarks` and after the verdict, show a "City Benchmark Comparison" section: a small table/list showing average gross yields for major cities so users can compare their result
    - Replace inline CalcField/Row with shared components from `@/components/calculators/shared/`
    - Replace inline verdict logic with VerdictBox component
    - Keep the same visual layout (cream card, 2-column grid)

    **BuyVsRentCalc.tsx improvements (TOOL-02):**
    - Make rent inflation adjustable (currently hardcoded 5%) -- add CalcField with default 5%, step 0.5
    - Make equity return rate adjustable (currently hardcoded 12%) -- add CalcField with default 12%, step 0.5
    - Replace hardcoded 7% registration cost with a note that says "Estimated at 7% -- use our Stamp Duty Calculator for exact numbers" and link to /tools/stamp-duty-calculator
    - Replace inline Field/Row with shared components
    - Replace inline verdict with VerdictBox

    **TotalCostCalc.tsx improvements (TOOL-03):**
    - Import `STAMP_DUTY_RATES` from `@/data/stamp-duty-rates` and replace the hardcoded `stampRates` object
    - Add a gender select field (Male/Female) that changes the stamp duty rate used
    - Populate the state dropdown from `STAMP_DUTY_RATES` array (all 28 states + 8 UTs instead of just 6)
    - Replace `Math.min(price * 0.01, 30000)` registration fee with the actual `registration` percentage and `regCap` from the data
    - Replace inline components with shared components
    - Add "Last updated: March 2026" note at bottom

    **Route pages:** Create 3 route page files (rental-yield, buy-vs-rent, total-cost). Each is a Server Component that:
    - Exports unique `Metadata` with tool-specific title, description, openGraph (see RESEARCH.md Pattern 1 for format)
    - Renders `<ToolPageWrapper title="..." description="..."><ComponentName /></ToolPageWrapper>`
    - Titles: "Rental Yield Calculator -- Real Rental Yield India | SquareMind", "Buy vs Rent Calculator -- Should You Buy or Rent in India? | SquareMind", "Total Cost of Ownership Calculator -- True Property Cost India | SquareMind"
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>3 existing calculators updated with improved data and UX. 3 route pages created. Each tool accessible at /tools/rental-yield, /tools/buy-vs-rent, /tools/total-cost. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build EMI Calculator and Stamp Duty Calculator with route pages</name>
  <files>
    src/components/calculators/EMICalculator.tsx,
    src/components/calculators/StampDutyCalc.tsx,
    src/app/(main)/tools/emi-calculator/page.tsx,
    src/app/(main)/tools/stamp-duty-calculator/page.tsx
  </files>
  <action>
    **EMICalculator.tsx (TOOL-07):**
    - "use client" component
    - Inputs (using shared CalcField): Loan Amount (default Rs.50,00,000), Annual Interest Rate (default 8.5%, step 0.1), Loan Tenure in years (default 20, min 1, max 30)
    - Calculate EMI using standard formula: `EMI = P * r * (1+r)^n / ((1+r)^n - 1)` where r = annual rate / 12 / 100, n = years * 12. Handle edge case: if rate is 0, EMI = principal / months.
    - Display results: Monthly EMI (large headline number), Total Interest Payable, Total Amount Payable (principal + interest), Interest-to-Principal Ratio
    - Show amortization schedule as a collapsible table (default collapsed, "Show amortization schedule" toggle). Table columns: Year, Opening Balance, Principal Paid, Interest Paid, Closing Balance. Aggregate by YEAR (not month) to keep the table reasonable for 20-30 year loans. Use `fmt()` helper with `Intl.NumberFormat('en-IN')`.
    - VerdictBox: if interest > principal, show warning "You'll pay more in interest than the loan itself"; if interest < 50% of principal, show positive "Interest-efficient loan"
    - **Reference values for verification:** Rs.50L at 8.5% for 20 years = Rs.43,391/month (approximately)
    - Style: same cream card layout as existing calculators, 2-column grid (inputs left, results right)

    **StampDutyCalc.tsx (TOOL-08):**
    - "use client" component
    - Import `STAMP_DUTY_RATES` from `@/data/stamp-duty-rates`
    - Inputs: Property Value (default Rs.1,00,00,000), State (dropdown populated from STAMP_DUTY_RATES), Gender (Male/Female dropdown)
    - Calculate: Stamp Duty = price * (male or female rate / 100), Registration Fee = price * (registration / 100) capped at regCap if set, Total = stamp duty + registration
    - Display: Stamp Duty amount, Registration Fee, Total Government Charges, Effective Rate %
    - Below results, show a comparison table of stamp duty rates across top 5 investment states (Maharashtra, Karnataka, Delhi, Telangana, UP) for context
    - If the selected state has a `notes` field, display it as an info note
    - Add "Last updated: March 2026" disclaimer
    - Style: consistent with other calculators

    **Route pages:** Create 2 route pages:
    - `emi-calculator/page.tsx`: metadata title "EMI Calculator -- Home Loan EMI Calculator India | SquareMind", description about calculating monthly EMI and amortization. Wrap EMICalculator in ToolPageWrapper.
    - `stamp-duty-calculator/page.tsx`: metadata title "Stamp Duty Calculator -- State-wise Stamp Duty Rates 2026 | SquareMind". Wrap StampDutyCalc in ToolPageWrapper.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>EMI Calculator produces correct results (Rs.50L at 8.5% for 20yr ~ Rs.43,391/mo). Stamp Duty Calculator shows correct state-wise rates with gender differentiation. Both accessible at their own URLs. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- 5 route pages exist under src/app/(main)/tools/
- EMI calculation: Rs.50,00,000 at 8.5% for 20 years ~ Rs.43,391/month
- Stamp duty: Maharashtra male on Rs.1Cr = Rs.6,00,000 (6%)
- Rental yield: city benchmark comparison visible
- Total cost: all 28+ states in dropdown, gender selection works
</verification>

<success_criteria>
3 existing calculators improved with real data and better UX. 2 new financial calculators built and working. All 5 have individual SEO-optimized URLs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-interactive-tools/05-02-SUMMARY.md`
</output>
